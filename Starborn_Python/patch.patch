--- a/ui/fishing_screen.py
+++ b/ui/fishing_screen.py
@@
 class FishingScreen(Screen):
     def __init__(self, zone_id: str, **kwargs):
-        super().__init__(name='fishing', **kwargs)
+        super().__init__(name='fishing', **kwargs)
         self.game = App.get_running_app().current_game
         self.fishing_manager = self.game.fishing_manager
         self.zone_id = zone_id
+        self._overlay = None

-    def on_enter(self, *args):
-        self._choose_rod()
+    def on_pre_enter(self, *args):
+        """Grab the blurred room snapshot & show our overlay."""
+        from kivy.app import App
+        from kivy.core.window import Window
+        from ui.background_snapshot import blurred_dimmed
+        from ui.menu_overlay import MenuOverlay
+
+        # ① snapshot the ScalableFloatLayout
+        tex = blurred_dimmed(self.ids.viewport)
+        App.get_running_app().last_room_tex = tex
+
+        # ② build & show the overlay
+        self._overlay = MenuOverlay(
+            default_tab=None,
+            title="Fishing",
+            background_tex=tex,
+        )
+        # when the overlay dismisses, jump back to explore
+        self._overlay.bind(on_dismiss=lambda *_:
+                          setattr(self.manager, "current", "explore"))
+        Window.add_widget(self._overlay)
+
+        # finally kick off rod selection
+        self._choose_rod()

     def _choose_rod(self):
         rods = []
@@
     def _return_to_explore(self):
         from kivy.app import App
         sm = App.get_running_app().screen_manager
         # go back to explore…
         sm.current = 'explore'
+        # remove our overlay if it’s still up
+        if self._overlay:
+            from kivy.core.window import Window
+            Window.remove_widget(self._overlay)
+            self._overlay = None
         # …and yank the FishingScreen out of the manager a moment later
         Clock.schedule_once(lambda dt: sm.remove_widget(self), 0.1)
