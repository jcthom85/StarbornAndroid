# Gameplay run

- Scenario / variant:
- Coverage IDs:
- Fixture revision:
- Build / commit (include dirty-worktree state):
- Device / Android version:
- Date / tester:
- Seed, if supported:
- Tutorials, audio, accessibility settings:
- Starting save / resources / loadout:
- Status: NOT RUN / PASSED / FAILED / BLOCKED

## Actions and observations

| Step | Expected | Actual | Evidence |
| --- | --- | --- | --- |
| | | | |

## Result

Record final quest/stage, inventory/rewards, onward route, save/reload outcome, and elapsed play time.
List criteria not exercised separately; do not count a partial run as a full pass.

## Defects

For each defect include reproduction steps, expected/actual behavior, repeatability, severity,
classification (game / fixture / instructions / environment), affected coverage IDs, and screenshot/log/save reference.

## Retest

Record the fix build and original/adjacent cases rerun. Preserve the original failure evidence.
